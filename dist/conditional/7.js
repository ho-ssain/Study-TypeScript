"use strict";
const id = 393937;
const hasID = !!id;
const isBlocked = true;
const day = "Sunday";
const isAdult = age >= 18 && hasID;
const isWeekend = day === "Sunday" || day === "Saterday";
const hasPermission = !isBlocked;
